const express=require("express");const authenticate=require("../middleware/authenticate");const requireAdmin=require("../middleware/adminMiddleware");const{requestPayment,getMyPayment,getPaymentSupport,submitProof}=require("../controllers/manualPaymentController");const{submitProof:submitStoredProof,getProof}=require("../controllers/paymentProofController");const{confirmPayment,listPendingPayments,rejectPayment}=require("../controllers/manualPaymentController");const router=express.Router();router.get("/support",getPaymentSupport);router.post("/request",authenticate,requestPayment);router.get("/mine/:token",authenticate,getMyPayment);router.post("/mine/:token/proof",authenticate,submitStoredProof);router.get("/proof/:id",authenticate,getProof);router.get("/admin/pending",authenticate,requireAdmin,listPendingPayments);router.patch("/admin/:id/confirm",authenticate,requireAdmin,confirmPayment);router.patch("/admin/:id/reject",authenticate,requireAdmin,rejectPayment);module.exports=router;
