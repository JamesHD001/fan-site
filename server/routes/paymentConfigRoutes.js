const express=require("express");const {getPublicPaymentOptions,upsertConfig}=require("../controllers/paymentConfigController");const authenticate=require("../middleware/authenticate");const requireAdmin=require("../middleware/adminMiddleware");const router=express.Router();router.get("/options",getPublicPaymentOptions);router.put("/config",authenticate,requireAdmin,upsertConfig);module.exports=router;
