const crypto=require("crypto");const Payment=require("../models/Payment");const PaymentConfig=require("../models/PaymentConfig");const User=require("../models/User");const Membership=require("../models/Membership");const MembershipPlan=require("../models/MembershipPlan");const Booking=require("../models/Booking");const MeetingType=require("../models/MeetingType");const Gift=require("../models/Gift");const GiftTransaction=require("../models/GiftTransaction");const {markManualPaymentSuccessful}=require("../services/manualPaymentService");const token=()=>`PAY-${new Date().toISOString().slice(0,10).replace(/-/g,"")}-${crypto.randomBytes(5).toString("hex").toUpperCase()}`;const tierOrder=["FAN","SUPPORTER","INSIDER","VIP"];const getSupportAdmin=async()=>User.findOne({role:"ADMIN",isPaymentSupport:true,isActive:true}).select("name username email profileImage");const getConfig=async()=>PaymentConfig.findOne({key:"default"}).lean();
const requestPayment=async(req,res)=>{try{const{type,itemId,scheduledFor,quantity,message,notes,paymentMethod="CRYPTO",cryptoCurrency,cryptoNetwork,giftCardBrand}=req.body;if(!["MEMBERSHIP","MEETING","GIFT"].includes(type)||!itemId)return res.status(400).json({success:false,message:"A valid payment type and item are required."});if(!["CRYPTO","GIFTCARD"].includes(paymentMethod))return res.status(400).json({success:false,message:"Choose a valid payment method."});const supportAdmin=await getSupportAdmin();if(!supportAdmin)return res.status(503).json({success:false,message:"Payment support is currently unavailable."});const config=await getConfig();const cryptoOptions=(config?.cryptoOptions||[]).filter(x=>x.isActive);const giftCardOptions=(config?.giftCardOptions||[]).filter(x=>x.isActive);let amount,currency="USD",membership=null,booking=null,giftTransaction=null,description="",crypto=null,giftCard=null;
if(paymentMethod==="CRYPTO"){if(!cryptoCurrency||!cryptoNetwork)return res.status(400).json({success:false,message:"Select a cryptocurrency and network."});const option=cryptoOptions.find(x=>x.currency===String(cryptoCurrency).toUpperCase()&&x.network===cryptoNetwork);if(!option)return res.status(400).json({success:false,message:"That cryptocurrency/network is not currently available."});crypto={currency:option.currency,network:option.network,walletAddress:option.walletAddress}}
if(paymentMethod==="GIFTCARD"){if(!giftCardBrand)return res.status(400).json({success:false,message:"Select a gift card brand."});const option=giftCardOptions.find(x=>x.brand===giftCardBrand);if(!option)return res.status(400).json({success:false,message:"That gift card is not currently available."});giftCard={brand:option.brand}}
if(type==="MEMBERSHIP"){const plan=await MembershipPlan.findOne({_id:itemId,isActive:true});if(!plan)return res.status(404).json({success:false,message:"Membership plan not found."});const existing=await Membership.findOne({user:req.user._id,status:"ACTIVE",expiresAt:{$gt:new Date()}});if(existing)return res.status(409).json({success:false,message:"You already have an active membership."});amount=plan.price;currency=plan.currency||"USD";description=`${plan.name} membership`;membership=await Membership.create({user:req.user._id,plan:plan._id,status:"PENDING"})}
if(type==="MEETING"){const meetingType=await MeetingType.findOne({_id:itemId,isActive:true});if(!meetingType)return res.status(404).json({success:false,message:"Meeting type not found."});const slot=new Date(scheduledFor);if(!scheduledFor||Number.isNaN(slot.getTime())||slot.getTime()<Date.now())return res.status(400).json({success:false,message:"A valid future meeting date/time is required."});const activeMembership=await Membership.findOne({user:req.user._id,status:"ACTIVE",expiresAt:{$gt:new Date()}}).populate("plan","minimumMeetingTier");if(!activeMembership)return res.status(403).json({success:false,message:"An active membership is required to book a meeting."});const requiredTier=meetingType.minimumMembershipTier;if(tierOrder.indexOf(activeMembership.plan.minimumMeetingTier)<tierOrder.indexOf(requiredTier))return res.status(403).json({success:false,message:`The ${meetingType.name} requires at least a ${requiredTier} membership.`});if(await Booking.findOne({scheduledFor:slot,status:{$in:["PENDING_PAYMENT","CONFIRMED","COMPLETED"]}}))return res.status(409).json({success:false,message:"That time slot is no longer available."});amount=meetingType.price;currency=meetingType.currency||"USD";description=`${meetingType.name} meeting`;booking=await Booking.create({user:req.user._id,meetingType:meetingType._id,reference:`BK-${Date.now()}-${crypto.randomBytes(4).toString("hex").toUpperCase()}`,scheduledFor:slot,notes:notes||"",status:"PENDING_PAYMENT"})}
if(type==="GIFT"){const gift=await Gift.findOne({_id:itemId,isActive:true});if(!gift)return res.status(404).json({success:false,message:"Gift not found."});const qty=Number.isInteger(Number(quantity))&&Number(quantity)>0?Math.min(Number(quantity),100):1;amount=gift.price*qty;currency=gift.currency||"USD";description=`${gift.name}${qty>1?` x${qty}`:""}`;giftTransaction=await GiftTransaction.create({user:req.user._id,gift:gift._id,reference:`GFT-${Date.now()}-${crypto.randomBytes(4).toString("hex").toUpperCase()}`,amount,quantity:qty,message:message||"",status:"PENDING_PAYMENT"})}
const paymentToken=token();const payment=await Payment.create({user:req.user._id,type,membership:membership?._id||null,booking:booking?._id||null,giftTransaction:giftTransaction?._id||null,reference:paymentToken,paymentToken,supportAdmin:supportAdmin._id,originalAmount:amount,originalCurrency:currency,amount,currency,exchangeRate:1,provider:"INTERNAL",paymentMethod,crypto,giftCard,status:"PENDING_PAYMENT",metadata:{description}});if(booking){booking.payment=payment._id;await booking.save()}if(giftTransaction){giftTransaction.payment=payment._id;await giftTransaction.save()}return res.status(201).json({success:true,message:"Payment request created.",payment:{id:payment._id,token:payment.paymentToken,reference:payment.reference,type:payment.type,amount:payment.originalAmount,currency:payment.originalCurrency,paymentMethod:payment.paymentMethod,status:payment.status,crypto:payment.crypto,giftCard:payment.giftCard,createdAt:payment.createdAt},support:supportAdmin})}catch(error){console.error("Manual payment request error:",error);return res.status(500).json({success:false,message:error.message||"Unable to create payment request."})}};
const getMyPayment=async(req,res)=>{try{const payment=await Payment.findOne({user:req.user._id,$or:[{paymentToken:req.params.token},{reference:req.params.token}]}).populate("supportAdmin","name username email profileImage");if(!payment)return res.status(404).json({success:false,message:"Payment request not found."});return res.json({success:true,payment})}catch(error){return res.status(500).json({success:false,message:"Unable to retrieve payment request."})}};
const getPaymentSupport=async(req,res)=>{const support=await getSupportAdmin();if(!support)return res.status(404).json({success:false,message:"Payment support is not currently available."});return res.json({success:true,support})};
const confirmPayment=async(req,res)=>{try{const payment=await Payment.findById(req.params.id);if(!payment)return res.status(404).json({success:false,message:"Payment request not found."});if(!payment.supportAdmin.equals(req.user._id))return res.status(403).json({success:false,message:"Only the designated payment support administrator can confirm this payment."});if(payment.status!=="PROOF_SUBMITTED")return res.status(400).json({success:false,message:"The member must submit payment proof before confirmation."});const result=await markManualPaymentSuccessful({paymentId:payment._id,adminId:req.user._id,adminNote:req.body.adminNote||""});return res.json({success:true,message:"Payment confirmed successfully. The member has been notified.",...result})}catch(error){return res.status(400).json({success:false,message:error.message||"Unable to confirm payment."})}};
const listPendingPayments=async(req,res)=>{try{const payments=await Payment.find({supportAdmin:req.user._id,status:{$in:["PENDING_PAYMENT","PROOF_SUBMITTED"]}}).populate("user","name username email").sort({createdAt:1});return res.json({success:true,payments})}catch(error){return res.status(500).json({success:false,message:"Unable to retrieve pending payments."})}};
const submitProof=async(req,res)=>{try{const payment=await Payment.findOne({user:req.user._id,$or:[{paymentToken:req.params.token},{reference:req.params.token}]});if(!payment)return res.status(404).json({success:false,message:"Payment request not found."});if(payment.status!=="PENDING_PAYMENT")return res.status(400).json({success:false,message:"This payment is not awaiting proof."});const{proofUrl,fileType,originalName}=req.body;if(!proofUrl)return res.status(400).json({success:false,message:"Payment proof is required."});payment.proof={fileUrl:proofUrl,fileType:fileType||"image",originalName:originalName||"payment-proof",uploadedAt:new Date()};payment.status="PROOF_SUBMITTED";await payment.save();return res.json({success:true,message:"Payment proof submitted for administrator review.",payment})}catch(error){return res.status(400).json({success:false,message:error.message||"Unable to submit payment proof."})}};
const rejectPayment=async(req,res)=>{try{const payment=await Payment.findById(req.params.id);if(!payment)return res.status(404).json({success:false,message:"Payment request not found."});if(!payment.supportAdmin.equals(req.user._id))return res.status(403).json({success:false,message:"Only the designated payment support administrator can reject this payment."});if(!["PENDING_PAYMENT","PROOF_SUBMITTED"].includes(payment.status))return res.status(400).json({success:false,message:"This payment cannot be rejected."});payment.status="REJECTED";payment.adminNote=String(req.body.adminNote||"Payment proof was rejected.").slice(0,1000);payment.verifiedBy=req.user._id;payment.verifiedAt=new Date();await payment.save();return res.json({success:true,message:"Payment request rejected.",payment})}catch(error){return res.status(400).json({success:false,message:error.message||"Unable to reject payment."})}};
const setPaymentSupport=async(req,res)=>{try{const user=await User.findById(req.params.id);if(!user||user.role!=="ADMIN")return res.status(404).json({success:false,message:"Administrator not found."});await User.updateMany({role:"ADMIN",isPaymentSupport:true},{$set:{isPaymentSupport:false}});user.isPaymentSupport=true;await user.save();return res.json({success:true,message:`${user.name} is now the designated payment support administrator.`})}catch(error){return res.status(500).json({success:false,message:"Unable to designate payment support."})}};
module.exports={requestPayment,getMyPayment,getPaymentSupport,confirmPayment,listPendingPayments,submitProof,rejectPayment,setPaymentSupport};
