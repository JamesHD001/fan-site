import{useEffect,useState}from'react';import{Link,useNavigate,useParams}from'react-router-dom';import{useAuth}from'../context/AuthContext';import'../styles/payment.css';const API=import.meta.env.VITE_API_URL||'http://localhost:5000/api';const money=(a,c='USD')=>new Intl.NumberFormat('en-US',{style:'currency',currency:c}).format(Number(a||0)/100);const MAX_PROOF_BYTES=5*1024*1024;const ALLOWED_PROOF_TYPES=['image/jpeg','image/png','image/webp','application/pdf'];export default function ManualPaymentPage(){const{token}=useAuth();const{token:paymentToken}=useParams();const navigate=useNavigate();const[payment,setPayment]=useState(null),[options,setOptions]=useState({cryptoOptions:[],giftCardOptions:[]}),[method,setMethod]=useState('CRYPTO'),[crypto,setCrypto]=useState(''),[network,setNetwork]=useState(''),[giftCard,setGiftCard]=useState(''),[proof,setProof]=useState(''),[proofName,setProofName]=useState(''),[proofType,setProofType]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(true);const load=async()=>{const[r,o]=await Promise.all([fetch(`${API}/payments/mine/${encodeURIComponent(paymentToken)}`,{headers:{Authorization:`Bearer ${token}`}}),fetch(`${API}/payment-config/options`)]);const d=await r.json(),od=await o.json();if(!r.ok||!d.success)throw new Error(d.message||'Unable to load payment request.');setPayment(d.payment);if(od.success){setOptions(od);if(od.cryptoOptions[0]){setCrypto(od.cryptoOptions[0].currency);setNetwork(od.cryptoOptions[0].network)}}};useEffect(()=>{load().catch(e=>setError(e.message)).finally(()=>setLoading(false))},[paymentToken,token]);const chooseProof=event=>{const file=event.target.files?.[0];setError('');setProof('');setProofName('');setProofType('');if(!file)return;if(!ALLOWED_PROOF_TYPES.includes(file.type))return setError('Payment proof must be a JPG, PNG, WEBP image or PDF receipt.');if(file.size>MAX_PROOF_BYTES)return setError('Payment proof is too large. Maximum size is 5 MB.');const reader=new FileReader();reader.onload=()=>{setProof(String(reader.result||''));setProofName(file.name);setProofType(file.type)};reader.onerror=()=>setError('Unable to read the selected proof file. Please try again.');reader.readAsDataURL(file)};const submitProof=async()=>{if(!proof)return setError('Please select a screenshot or receipt before submitting.');setBusy(true);setError('');try{const r=await fetch(`${API}/payments/mine/${encodeURIComponent(paymentToken)}/proof`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({proofUrl:proof,fileType:proofType,originalName:proofName})}),d=await r.json();if(!r.ok||!d.success)throw new Error(d.message||'Unable to submit proof.');setPayment(d.payment)}catch(e){setError(e.message)}finally{setBusy(false)}};const selectedCrypto=options.cryptoOptions.find(x=>x.currency===crypto&&x.network===network);if(loading)return <main className="payment-page"><div className="payment-card"><p>Loading payment request…</p></div></main>;if(error&&!payment)return <main className="payment-page"><div className="payment-card error-card"><h1>Payment request unavailable</h1><p>{error}</p><Link className="primary-button" to="/">Return Home</Link></div></main>;const success=payment?.status==='SUCCESS',submitted=payment?.status==='PROOF_SUBMITTED',rejected=payment?.status==='REJECTED';return <main className="payment-page"><div className="payment-card"><p className="eyebrow">{success?'PAYMENT CONFIRMED':submitted?'PROOF UNDER REVIEW':rejected?'PAYMENT REJECTED':'CHOOSE PAYMENT METHOD'}</p><h1>{success?'Payment confirmed.':submitted?'Payment proof submitted.':rejected?'Payment proof needs attention.':'Complete your payment'}</h1><div className="membership-summary"><span>Payment token<strong>{payment.paymentToken}</strong></span><span>Item<strong>{payment.metadata?.description||payment.type}</strong></span><span>Amount<strong>{money(payment.originalAmount,payment.originalCurrency)}</strong></span></div>{success?<><p>Your payment has been confirmed by the designated support administrator. The associated purchase has been completed and your account has been updated.</p><Link className="primary-button" to={payment.type==='MEMBERSHIP'?'/membership':payment.type==='MEETING'?'/meetings':'/gifts'}>View Purchase</Link></>:submitted?<p>Your proof is now with the designated payment support administrator. Do not send another payment while this request is being reviewed.</p>:<><div className="payment-method-tabs"><button className={method==='CRYPTO'?'active':''} onClick={()=>setMethod('CRYPTO')}>🪙 Crypto</button><button className={method==='GIFTCARD'?'active':''} onClick={()=>setMethod('GIFTCARD')}>🎁 Gift Cards</button></div>{method==='CRYPTO'?<section className="payment-option-panel"><h2>Select cryptocurrency</h2><div className="payment-choice-grid">{options.cryptoOptions.map(x=><button key={`${x.currency}-${x.network}`} className={crypto===x.currency&&network===x.network?'selected':''} onClick={()=>{setCrypto(x.currency);setNetwork(x.network)}}>{x.currency}<small>{x.network}</small></button>)}</div>{selectedCrypto&&<div className="wallet-box"><p>Send your payment using</p><strong>{selectedCrypto.currency} · {selectedCrypto.network}</strong><label>Wallet address<input readOnly value={selectedCrypto.walletAddress}/></label><button className="secondary-button" onClick={()=>navigator.clipboard?.writeText(selectedCrypto.walletAddress)}>Copy wallet address</button><p className="muted">Contact <strong>{payment.supportAdmin?.name}</strong> if you need payment instructions. Include token <strong>{payment.paymentToken}</strong>.</p></div>}</section>:<section className="payment-option-panel"><h2>Select gift card</h2><div className="payment-choice-grid">{options.giftCardOptions.map(x=><button key={x._id} className={giftCard===x.brand?'selected':''} onClick={()=>setGiftCard(x.brand)}>{x.brand}</button>)}</div>{giftCard&&<div className="wallet-box"><h3>{giftCard}</h3><p>{options.giftCardOptions.find(x=>x.brand===giftCard)?.instructions}</p><p className="muted">Contact <strong>{payment.supportAdmin?.name}</strong> and provide token <strong>{payment.paymentToken}</strong> for the exact gift-card instructions.</p></div>}</section>}<section className="proof-box"><h2>I've completed payment</h2><p>Upload a screenshot of the transaction or your payment receipt. The designated administrator will review it before your purchase is completed.</p><label className="proof-upload"><span>Payment screenshot / receipt</span><input type="file" accept="image/jpeg,image/png,image/webp,application/pdf" onChange={chooseProof}/><small>JPG, PNG, WEBP or PDF · maximum 5 MB</small></label>{proof&&<div className="proof-selected"><strong>{proofName}</strong><span>{proofType==='application/pdf'?'PDF receipt':'Image selected'}</span><button type="button" className="secondary-button" onClick={()=>{setProof('');setProofName('');setProofType('')}}>Remove</button></div>}<button className="primary-button" disabled={busy||!proof} onClick={submitProof}>{busy?'Submitting…':'Submit payment proof'}</button></section>{error&&<p className="auth-error">{error}</p>}</>}<div className="payment-actions"><button className="secondary-button" onClick={()=>navigate(-1)}>Go Back</button></div></div></main>}
