import{useEffect,useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';importMembershipCard from'../components/membership/MembershipCard';import'../styles/membership.css';const API=import.meta.env.VITE_API_URL||'http://localhost:5000/api';const money=(a,c='USD')=>new Intl.NumberFormat('en-US',{style:'currency',currency:c,maximumFractionDigits:0}).format(Number(a||0)/100);export default function MembershipPage(){const{token,user}=useAuth();const navigate=useNavigate();const[plans,setPlans]=useState([]),[membership,setMembership]=useState(null),[card,setCard]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[purchasing,setPurchasing]=useState(null);useEffect(()=>{(async()=>{try{const p=await fetch(`${API}/memberships/plans`),pd=await p.json();if(p.ok&&pd.success)setPlans(pd.plans||[]);if(token){const m=await fetch(`${API}/memberships/me`,{headers:{Authorization:`Bearer ${token}`}}),md=await m.json();setMembership(md.membership||null);if(md.success&&md.membership?.status==='ACTIVE'){const c=await fetch(`${API}/memberships/card`,{headers:{Authorization:`Bearer ${token}`}}),cd=await c.json();if(c.ok&&cd.success)setCard(cd.card)}}}catch{setError('Unable to load membership information.')}finally{setLoading(false)}})()},[token]);const startPurchase=async planId=>{if(!token)return navigate('/login');setPurchasing(planId);setError('');try{const r=await fetch(`${API}/memberships/initialize`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({planId})}),d=await r.json();if(!r.ok||!d.success)throw new Error(d.message||'Unable to create payment request.');navigate(`/payment/pending/${encodeURIComponent(d.payment.token)}`)}catch(e){setError(e.message)}finally{setPurchasing(null)}};const activePlan=membership?.plan&&typeof membership.plan==='object'?membership.plan:plans.find(p=>p._id===membership?.plan),active=membership?.status==='ACTIVE';if(loading)return <main className="dashboard-page"><section className="dashboard-hero"><p>Loading your community access…</p></section></main>;return <main className="dashboard-page"><header className="dashboard-hero page-container"><div><span className="eyebrow">MEMBERSHIP HUB</span><h1>{active?'Your membership.':'Find your place in the community.'}</h1><p>{active?'Manage your tier, card and renewal from one place.':'Compare the available tiers and choose the level of access that fits you.'}</p></div></header>{error&&<div className="membership-alert page-container">{error}</div>}{active&&activePlan&&<section className="dashboard-section page-container"><div className="dashboard-grid"><article className="dashboard-panel"><span className="eyebrow">CURRENT TIER</span><h2>{activePlan.name}</h2><p>{activePlan.description}</p><div className="metric-row"><div><span>EXPIRES</span><strong>{membership.expiresAt?new Date(membership.expiresAt).toLocaleDateString():'—'}</strong></div><div><span>MEMBER</span><strong>{card?.memberName||user?.name||user?.username||'Member'}</strong></div></div><Link className="text-link" to="/settings">Manage account →</Link></article>{card&&<article className="dashboard-panel card-panel"><div><span className="eyebrow">DIGITAL CARD</span><h2>Carry your membership.</h2></div><MembershipCard card={card} onPrint={()=>window.print()}/></article>}</div></section>}<section className="dashboard-section page-container"><div className="section-heading"><div><span className="eyebrow">{active?'CHANGE OR RENEW':'MEMBERSHIP TIERS'}</span><h2>{active?'Choose your next tier.':'Choose your level of access.'}</h2></div><Link className="text-link" to="/membership/payments">Payment history →</Link></div><div className="dashboard-plans">{plans.map(plan=><article className={`dashboard-plan ${plan.name==='Insider'?'featured':''}`} key={plan._id}><span className="eyebrow">{plan.badge||plan.name}</span><h3>{plan.name}</h3><strong>{money(plan.price,plan.currency||'USD')}</strong><small> / {(plan.durationUnit||'YEAR').toLowerCase()}</small><p>{plan.description}</p><ul>{(plan.benefits||[]).slice(0,5).map(b=><li key={b}>✓ {b}</li>)}</ul><button className="button button-primary" disabled={purchasing===plan._id||((activePlan?._id===plan._id)&&active)} onClick={()=>startPurchase(plan._id)}>{purchasing===plan._id?'Creating payment request…':activePlan?._id===plan._id?'Current membership':active?`Renew ${plan.name}`:`Continue to payment`}</button></article>)}</div></section><div className="dashboard-links page-container"><Link to="/meetings">Explore private meetings →</Link><Link to="/community">Enter the community →</Link></div></main>}
