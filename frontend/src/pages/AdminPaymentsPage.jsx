import {useEffect,useState} from'react';import{Link}from'react-router-dom';import{useAuth}from'../context/AuthContext';import'../styles/admin.css';const API=import.meta.env.VITE_API_URL||'http://localhost:5000/api';const money=(a,c='USD')=>new Intl.NumberFormat('en-US',{style:'currency',currency:c}).format(Number(a||0)/100);export default function AdminPaymentsPage(){const{token,user}=useAuth();const[payments,setPayments]=useState([]),[support,setSupport]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(true),[busy,setBusy]=useState(null);const load=async()=>{setLoading(true);try{const[r,s]=await Promise.all([fetch(`${API}/admin/payments/pending`,{headers:{Authorization:`Bearer ${token}`}}),fetch(`${API}/payments/support`)]);const d=await r.json(),sd=await s.json();if(!r.ok||!d.success)throw new Error(d.message||'Unable to load payment requests.');setPayments(d.payments||[]);if(sd.success)setSupport(sd.support)}catch(e){setError(e.message)}finally{setLoading(false)}};useEffect(()=>{if(token&&user?.role==='ADMIN')load()},[token,user?.role]);const action=async(id,type)=>{setBusy(id);try{const r=await fetch(`${API}/admin/payments/${id}/${type}`,{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({adminNote:type==='reject'?'Payment proof rejected by support.':'Payment proof verified and payment received.'})});const d=await r.json();if(!r.ok||!d.success)throw new Error(d.message||'Unable to update payment.');await load()}catch(e){setError(e.message)}finally{setBusy(null)}};if(user?.role!=='ADMIN')return <main className="placeholder-page"><h1>Access denied</h1><Link to="/">Return home</Link></main>;return <main className="admin-page page-container"><header className="page-header"><p className="eyebrow">PAYMENT OPERATIONS</p><h1>Manual payment requests</h1><p className="muted">Review payment proofs before confirming a purchase.</p><div className="admin-links"><Link className="secondary-button" to="/admin">Dashboard</Link><Link className="secondary-button" to="/admin/manage">Management</Link></div></header>{support&&<section className="admin-revenue"><span className="eyebrow">DESIGNATED SUPPORT</span><h2>{support.name}</h2><p>{support.email} · @{support.username}</p></section>}{error&&<p className="auth-error">{error}</p>}{loading?<p>Loading payment requests…</p>:payments.length===0?<section className="dashboard-panel"><h2>No payment requests awaiting action</h2><p className="muted">New requests will appear here.</p></section>:<section className="admin-table-wrap"><table><thead><tr><th>Member</th><th>Purchase</th><th>Method</th><th>Amount</th><th>Token</th><th>Proof</th><th>Action</th></tr></thead><tbody>{payments.map(p=><tr key={p._id}><td>{p.user?.name||p.user?.username}<small>{p.user?.email}</small></td><td>{p.metadata?.description||p.type}</td><td>{p.paymentMethod==='CRYPTO'?`${p.crypto?.currency||''} ${p.crypto?.network||''}`:`Gift card: ${p.giftCard?.brand||''}`}</td><td>{money(p.originalAmount,p.originalCurrency)}</td><td><code>{p.paymentToken}</code></td><td>{p.proof?.fileUrl?<a href={p.proof.fileUrl} target="_blank" rel="noreferrer">View proof</a>:'Not submitted'}</td><td>{p.status==='PROOF_SUBMITTED'?<div className="admin-actions"><button disabled={busy===p._id} onClick={()=>action(p._id,'confirm')}>{busy===p._id?'Working…':'Confirm payment'}</button><button className="secondary-button" disabled={busy===p._id} onClick={()=>action(p._id,'reject')}>Reject</button></div>:<span className="muted">Awaiting proof</span>}</td></tr>)}</tbody></table></section>}</main>}
